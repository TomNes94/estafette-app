import MainPage from "./components/MainPage";
import LoginPage from "./components/LoginPage";

export const routes = {
    "/main": {
        component: MainPage
    },
    "/login": {
        component: LoginPage
    }
};
